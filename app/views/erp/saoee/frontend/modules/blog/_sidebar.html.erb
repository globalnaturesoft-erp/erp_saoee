<aside class="widget-area widget-sidebar">
    <% if false %>
        <div class="widget widget_search">
            <form class="search-form" method="GET" action="#">
                <input class="search-field" type="text" placeholder="Search here...">
                <button class="search-submit" type="submit">
                    <span class="ti-search"></span>
                </button>
            </form>
        </div>
    <% end %>
    <% if !@categories.empty? %>
        <div class="widget widget_categories">
            <h4 class="widget-title"><%= t('.categories') %></h4>
            <ul>
                <% @categories.each do |cat| %>
                    <li class="cat-item">
                        <%= link_to cat.name, erp_saoee.blog_with_category_path(url_friendly(cat.name), cat.id) %>
                        <span class="cat-count">(<%= cat.get_articles.count %>)</span>
                    </li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div class="widget widget_recent_entries">
        <h4 class="widget-title"><%= t('.newest_blogs') %></h4>
        <ul>
            <% @newest_blogs.each do |post| %>
                <li>
                    <%= link_to "<i class=\"zmdi zmdi-collection-text zmdi-hc-fw\"></i> #{post.name}".html_safe, blog_link(post) %>
                </li>
            <% end %>
        </ul>
    </div>
    <% projects = Erp::Projects::Project.newest_projects(9) %>
    <% if projects.count >= 3 %>
        <div class="widget widget_instagram">
            <h4 class="widget-title"><%= t('.latest_projects') %></h4>
            <ul class="widget_instagram_list">
                <% projects.each do |project| %>
                    <li>
                        <%= link_to (image_tag project.image.small), project_link(project), title: project.name %>
                    </li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <% if false %>
        <div class="widget widget_tag_cloud">
            <h4 class="widget-title"><%= t('tags') %></h4>
            <div class="tagcloud">
                <ul class="wp-tag-cloud">
                    <li>
                        <a href="#">a</a>
                    </li>
                    <li>
                        <a href="#">b</a>
                    </li>
                </ul>
            </div>
        </div>
    <% end %>
</aside>